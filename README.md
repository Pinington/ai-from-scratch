# Model From Scratch

An AI model designed to classify a point's position relative to functions. The data is random points so I can train for as long as I want, making the model pretty accurate. After training the model, we user the weights and biases to deduce the actual slope of the functions used for training. It usually gets results correct within a decimal point.
